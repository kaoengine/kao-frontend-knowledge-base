function identity(arg) {
    return arg;
  }
  
  identity('hello');
  
  // interface User {
  //   name: string;
  //   email?: string;
  // }
  
  // function getUser(id: number) {
  //   return fetch(`/users/${id}`).then(r => r.json());
  // }
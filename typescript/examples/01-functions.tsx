// import { } from "./sample";

const add = (a, b) => a + b;

add('hello', 'world');

// const addMany = (...numbers: Array<number>): number => numbers.reduce(add);

// addMany([2, 4, 5, 6]);

// interface User {
//   name: string;
//   email?: string;
// }

// const getUserName = (user: User) => user;
// const getUserEmail = (user: User) => user;

// getUserName({ name: "James", email: "james@example.com" });